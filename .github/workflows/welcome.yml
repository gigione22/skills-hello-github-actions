name: Welcome Workflow Gigi!

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - name: Say welcome
        run: echo "👋 Welcome to my GitHub Actions Gigione workflow!"
